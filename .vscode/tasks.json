{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build debug",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "zig build",
            "problemMatcher": [],
            
        },
        {
            "label": "build release",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "command": "zig build -Drelease-safe=true",
            "problemMatcher": []
        }
    ]
}